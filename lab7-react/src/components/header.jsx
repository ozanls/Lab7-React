import Nav from './nav';
import Logo from './logo'

export default function Header() {
    //Header component//
    return(
        <header id="header">
            <Logo />
            <Nav />
        </header>
    );
}